import { Layout } from "antd";
import styled from "styled-components";

const SimpleLayoutWrapper = styled(Layout)`
  .demo-logo-vertical {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    overflow: hidden;
    margin-left: auto;
    margin-right: auto;
  }
`;

export default SimpleLayoutWrapper;
