import { Modal } from "antd";
import styled from "styled-components";

const MediaPageWrapper = styled.div``;

export const ModalUploadMediaWrapper = styled(Modal)``

export default MediaPageWrapper;
